#:kivy 2.0.0
<Screen>:
    BoxLayout:
        orientation: 'horizontal'
        size: root.size
        canvas.before:
            Color:
                rgba: 1,1,1,0.8 # 青色
            Rectangle:
                pos: self.pos
                size: self.size
        Edit_Auto:
            size_hint_x: 0.5
        Preview:
            size_hint_x: 0.5

<Edit_Auto>:
    BoxLayout:
        orientation: 'vertical'
        size: root.size
        pos: root.pos
        BoxLayout:
            TextInput:
                size_hint_y: 0.8
                text: 'ファイルパス'
            Button:
                size_hint_y: 0.1
                text: '…'
            Button:
                size_hint_y: 0.1
                text: '実行'

<Preview>:
    BoxLayout:
        orientation: 'vertical'
        size: root.size
        pos: root.pos
        BoxLayout:
            Button:
                text: 'right'


保存
import kivy
from kivy.app import App

from kivy.core.text import LabelBase, DEFAULT_FONT  # 追加分
from kivy.resources import resource_add_path  # 追加分

#resource_add_path('c:/Windows/Fonts')  # 追加分
#LabelBase.register(DEFAULT_FONT, 'msgothic.ttc')  # 追加分

#import japanese_kivy
#Builder.load_file('./autoedit.kv')

class Preview(kivy.uix.widget.Widget):
    pass
class Edit_Auto(kivy.uix.widget.Widget):
    pass

class Screen(kivy.uix.widget.Widget):
    pass

class AutoeditApp(kivy.app.App):
    def build(self):
        return Screen()
    

if __name__ == '__main__':
    AutoeditApp().run()
